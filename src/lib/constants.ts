export const BEHAVIOR_CATEGORIES = {
    POSITIVE: 'positive',
    NEGATIVE: 'negative',
  } as const;
  
  export const APPROVAL_STATUS = {
    PENDING: 'pending',
    APPROVED: 'approved',
    REJECTED: 'rejected',
  } as const;
  
  export const DEFAULT_BEHAVIOR_SCORE = 100;